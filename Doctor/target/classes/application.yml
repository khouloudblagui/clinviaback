eureka:
  instance:
    hostname: localhost

  client:
    service-url:
      defaultZone: http://localhost:8761/eureka  # Eureka server URL

server:
  port: 8087  # Port for the Doctor microservice

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: khouloudblagui61@gmail.com
    password: kuvp wehr rmol hcpn  # Consider using environment variables or secrets management for the password
    properties:
      mail:
        smtp:
          ssl:
              trust: smtp.gmail.com
          auth: true
          starttls:
            enable: true
          connectiontimeout: 100000  # Adjusted to 100000 ms (100 seconds)
          timeout: 100000           # Adjusted to 100000 ms (100 seconds)
          writetimeout: 100000      # Adjusted to 100000 ms (100 seconds)

  application:
    name: doctor-service  # Service name for Eureka

  config:
    import: optional:configserver:http://localhost:8888  # Config server (optional)

  datasource:
    url: jdbc:mysql://localhost:3306/databasedoctor?createDatabaseIfNotExist=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update  # Updates database schema automatically
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect

  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB  # Minimum file size before buffering to disk
      max-file-size: 200MB      # Max file size per upload
      max-request-size: 215MB   # Max total request size for multipart uploads

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG  # Logs detailed web activity

# Authentication service configuration for Feign clients
auth:
  service:
    url: http://localhost:8083/api/v1/auth  # Authentication service URL
